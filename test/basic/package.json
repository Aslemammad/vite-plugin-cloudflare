{
  "main": "./dist/index.js",
  "scripts": {
    "build": "rimraf dist/ && vite-plugin-cloudflare build ./index.ts --outputDir ./dist/",
    "run": "miniflare"
  },
  "devDependencies": {
    "miniflare": "^1.4.1"
  }
}
